interface:
  display_name: "Gemini CLI 可见 TUI 开发"
  short_description: "由 Issue 任务自治驱动唯一 Gemini TUI 并独立验收"
  default_prompt: "使用 $gemini-cli-development 在唯一活动的可见 TUI 中驱动 Gemini CLI 完成开发，再由主 Agent 独立 Review 与验证；TUI 异常退出时由当前任务自主启动唯一替代 TUI，只在委托最终结束时通知父会话。"
