interface:
  display_name: "Agent 任务轻量监工"
  short_description: "隔离并行 worker，先完成先验收，监工独立闭环"
  default_prompt: "使用 $agent-task-supervisor 按两层拓扑推进：监工下发最小合同，再创建隔离的 GPT-5.6 Sol worker 自主计划、开发和测试；每个任务使用独立 cursor 与状态文件，任一完成就立即独立验收和交付，不等待同批全部结束。"
