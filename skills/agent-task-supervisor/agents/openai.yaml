interface:
  display_name: "Agent 任务轻量监工"
  short_description: "三层监工、双重验收、Issue 关闭和后续任务续跑"
  default_prompt: "使用 $agent-task-supervisor 按三层拓扑推进：会话只向 child 下发，每次下发后由直接父层启动 30 分钟本地 monitor；Issue 关闭后由 Epic 读取 Evidence 并启动后续 ready Issue。"
